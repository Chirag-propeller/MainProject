import React from 'react'
import { Agent } from '../types'
import { Campaign } from '../types'

const CampaignGoal = ({campaign, agents}: {campaign: Campaign, agents: Agent[]}) => {
  return (
    <div>CampaignGoal</div>
  )
}

export default CampaignGoal